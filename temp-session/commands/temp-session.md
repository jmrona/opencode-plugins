---
description: Mark this session as temporary — deleted on next opencode start. Run it from a fresh session (or straight from home).
---

The temp-session plugin has processed this command (a toast shows the result). Reply with exactly this and nothing else: "Done — check the toast for the result."
